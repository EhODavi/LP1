algoritmo "Exercicio 7-Revisao"

var
resposta:caracter
numero1:real
inicio
repita
   escreval("Informe um numero: ")
   leia(numero1)
   escreval("O logaritmo do numero é : 10 ^",log(numero1))
   escreval("Deseja continuar: ")
   leia(resposta)
ate resposta = "nao"
fimalgoritmo
