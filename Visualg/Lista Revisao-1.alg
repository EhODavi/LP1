algoritmo "Lista Revisão-1"

var
resultado,contador:inteiro
inicio
resultado <- 0
contador <- 0
enquanto contador <= 5 faca
   resultado <- resultado * contador
   contador <- contador + 1
fimenquanto
escreval("O resultado é")
escreva(resultado)
fimalgoritmo
